QuestEconomy v1.3.1 — small fixes (7 changed files).
- Coin Mint: every button now converts ONE coin per click (merge + break), clearer labels.
- Teleport/buyhome costs now say "X Copper Coins".

Deploy: extract, copy the "src" folder + pom.xml into your Version-2 repo clone (Replace All),
then GitHub Desktop -> Commit -> Push. Build -> QuestEconomy-1.3.1.jar -> replace jar -> restart.
/qadmin version should read "Pass 3.1 — build 5".
