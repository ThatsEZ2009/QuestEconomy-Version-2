QuestEconomy v1.5.0 — adds custom Java-only GUI backgrounds for the Quest Board & Coin Mint,
plus the v1.4.0 homes GUI. NOTE: after building, re-download BOTH the jar AND the resource pack.
The GUI background alignment (ascent/offset) is a first attempt and will likely need tuning.
